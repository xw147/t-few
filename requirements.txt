# ---------------------------------------------------------------------------
# T-Few dependencies, modernized for NVIDIA L40S (Ada / sm_89).
#
# NOTE: two packages are installed separately by setup_tfew.sh, NOT here:
#   - torch        -> needs the CUDA 11.8 wheel index (see the script)
#   - promptsource -> installed with --no-deps so it can't downgrade datasets
# ---------------------------------------------------------------------------

pytorch-lightning==1.9.5     # keeps T-Few's Lightning-1.5-era API mostly working
torchmetrics==0.11.4
transformers==4.30.2
accelerate==0.20.3           # required by from_pretrained(..., low_cpu_mem_usage=True)
datasets==2.14.6
sentencepiece==0.1.99
deepspeed==0.9.5             # ZeRO-3 / CPU offload, for fitting T0-11B
scikit-learn==1.3.2
scipy==1.10.1
psutil==5.9.5
tensorboard==2.13.0          # Lightning's default logger
protobuf==3.20.3             # pinned <3.21 to avoid the T5-tokenizer / TB breakage

# Transitive deps pinned to 2023-era versions: their newest releases removed
# APIs that datasets 2.14 / transformers 4.30 still call.
pyarrow==12.0.1              # >=15 removed pa.PyExtensionType (breaks datasets)
huggingface-hub==0.16.4     # newer hub removed cached_download (breaks transformers 4.30)
fsspec==2023.6.0            # newer fsspec breaks datasets 2.14

# promptsource needs jinja2 at runtime; pandas/pyyaml come in via datasets
jinja2==3.1.2
